#VRML V2.0 utf8

PROTO MySphere [] { Shape {
  appearance Appearance { material Material { diffuseColor 1 1 0 } }
  geometry Sphere { radius 5.0 } }
}

# this shouldn't be allowed, there's no "whatever" field
MySphere { whatever 1 }
