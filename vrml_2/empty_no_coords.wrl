#VRML V2.0 utf8

# Use a couple of coordinate-based nodes without coord field
# (or set explicity as NULL, which is the same).
# This must be handled without errors.

Shape { geometry IndexedFaceSet { } }
Shape { geometry IndexedLineSet { } }
Shape { geometry PointSet { } }